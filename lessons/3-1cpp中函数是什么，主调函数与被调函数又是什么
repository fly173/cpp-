# 3-1 C++ 中函数是什么，主调函数与被调函数又是什么

## 一、C++ 中函数是什么

函数（Function）是一段**可重复使用**的代码，用来完成某个具体任务。

函数的作用：

- 把大问题拆成小问题，代码更清晰。
- 避免重复写相同逻辑。
- 提高代码可维护性与可读性。

一个完整函数通常包括：

- 返回值类型（如 `int`、`void`）
- 函数名
- 参数列表
- 函数体

示例：

```cpp
int add(int a, int b) {
	return a + b;
}
```

这表示：`add` 函数接收两个 `int` 参数，返回它们的和。

## 二、什么是主调函数

主调函数（Caller）就是**发起函数调用**的函数。

例如在下面代码中，`main` 调用了 `add`，所以 `main` 是主调函数：

```cpp
int main() {
	int result = add(3, 5);
	return 0;
}
```

## 三、什么是被调函数

被调函数（Callee）就是**被其他函数调用**的函数。

在上面的例子里，`add` 被 `main` 调用，所以 `add` 是被调函数。

## 四、主调函数与被调函数的关系

- 主调函数把参数传给被调函数。
- 被调函数执行任务后，把结果返回给主调函数（如果有返回值）。
- 一个函数既可以是主调函数，也可以是被调函数，取决于调用关系。

例如：

- `main` 调用 `A`
- `A` 再调用 `B`

那么：

- 对 `A` 来说，`main` 是它的主调函数，`B` 是它调用的被调函数。

## 五、小结

- 函数：完成特定任务的代码块。
- 主调函数：调用别人的函数。
- 被调函数：被别人调用的函数。

掌握这三个概念后，后续学习参数传递、返回值、作用域和函数嵌套调用会更容易。
